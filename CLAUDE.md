# Claude Boot Adapter

Welcome, Claude. This is not a product repository; it is Eddie's Coding Agent Workspace (home base).

Before executing any commands or editing files:
1. Read the universal boot protocol and guidelines in [AGENTS.md](file:///d:/eddie-agents/coding-agent-workspace/AGENTS.md).
2. Read the active project status in [.agents/state.json](file:///d:/eddie-agents/coding-agent-workspace/.agents/state.json).
3. Activate the personas in `.agents/personas/`, rules in `.agents/rules/`, and skills in `.agents/skills/` appropriate for your current task.
4. Shift your operating context to the target product repository before modifying any product files.
