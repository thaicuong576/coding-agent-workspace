# Agent Handoff

## Project

`<project-or-feature-name>`

## Target Repo

`<absolute-path>`

## Build Mode

`Level 3 Multi-Agent Build`

## Agent Role

`<planner | frontend | backend | agent-workflow | data | qa | reviewer | deployer | other>`

## Task Packet

Goal:

`<what this agent should accomplish>`

Inputs:

- `<spec, branch, file, issue, artifact, or prior handoff>`

Expected outputs:

- `<files, tests, docs, screenshots, reports, or PR>`

## Boundaries

Do touch:

- `<area>`

Do not touch:

- `<area>`

## Acceptance Checks

- `<check>`
- `<check>`

## Current State

`<what is already done and what remains>`

## Handoff Notes

`<important context for the next agent>`
