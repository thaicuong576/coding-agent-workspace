# Shared Commands

Put reusable command recipes here.

Commands should be tool-neutral when possible. If a runtime needs a specific format, add an adapter under `.codex/commands/` or `.claude/commands/`.

