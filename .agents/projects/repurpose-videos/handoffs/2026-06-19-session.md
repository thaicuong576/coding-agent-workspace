# Handoff Log: 2026-06-19 Session

## Accomplishments
* **Nunito Black Glow Style**: Updated subtitle style to the custom "Nunito Black Glow" dual-layer visual effect.
* **Aesthetic Optimization**:
  - Identified why the text in the 1080p landscape screenshot looked ugly/blobby:
    1. Landscape font size was hardcoded to a tiny `FontSize=18`.
    2. Outlines (`Outline=4` and `Outline=10`) were hardcoded and too thick for size 18, swallowing the letters.
    3. Letters fused together due to thick outlines.
  - Implemented dynamic proportional font size (`font_size = max(1, round(0.034 * height))`) for BOTH portrait and landscape.
  - Implemented proportional outlines: `text_outline` (11% of font size) and `glow_outline` (24% of font size).
  - Added `Spacing=1.5` to separate characters and prevent outline-blob merging.
  - Raised the vertical margin to `MarginV=18` for better safe-area breathing room.
* **Blur Issue Diagnostics**:
  - Confirmed 7-second test videos are too short for stable OCR density walk, leading to tall blur bands.
* **End-to-End Test**: Rendered `"C:\Users\ASUS\OneDrive\Desktop\video test 3.mp4"` successfully to `"outputs/video_test_3_repurposed.mp4"` with the optimized settings.

## Status & Git State
* Active project: `repurpose-videos`
* Git branch: `v5`
* Compilation check: passed.
* Video render test: passed with code `0`.

## Subtitle Consistency Validation (1:45 PM Update)
* **Conditional Vocal Separation**: Optimized the pipeline orchestration to skip vocal isolation when Siren TTS credentials are not provided. This speeds up visually focused runs (like subtitle styling iteration) on CPU from over 5 minutes down to ~1 minute.
* **Test Consistency Video**: Executed the end-to-end video pipeline on a longer test video `test_consistent.mp4` with masking and Siren bypassed, outputting `test_consistent_output.mp4` to the Desktop.
* **Aesthetics & Readability Check**:
  - Auto-detected the subtitle zone at Y = `907 to 1026`.
  - Extracted test frames at 10s, 30s, and 50s.
  - Verified that the "Nunito Black Glow" dual-layer styling remains visually clean, centered, and proportional across the video duration.
  - Placed the extracted frames and details in a dedicated validation report: `subtitle_validation_report.md` in the brain app data directory.
* **Pipeline Caching (`--cache-dir`)**:
  - Implemented intermediate caching of files (`audio.wav`, `audio.zh.srt`, `coords.txt`, `audio.vi.srt`, `bgm.wav`, `tts_voice.wav`) to skip heavy pipeline stages during testing/QA.
  - Verified a second run with cache takes under 8 seconds instead of minutes.

## Status & Git State
* Active project: `repurpose-videos`
* Git branch: `v5`
* Compilation check: passed.
* Video render test: passed with code `0`.

## Next Steps
1. **Commit & Push**: Commit the new `--cache-dir` parameter and code changes to `v5` branch.
2. **Review Open Backlog Items**:
   - Address TTS duration mismatch for long subtitle cues.
   - Add static region removal for top/corner watermarks.
   - Improve temporal consistency for inpainting on motion-heavy footage.
3. **QA Testing**: Use the new `--cache-dir` system to test subtitle alignment and inpainting locally without hitting API/model rate limits or delays.

## Command to Resume / Test Caching
```powershell
.venv\Scripts\python.exe -u repurpose.py -i "uploads/test_video.mp4" -o "outputs/test_video_rebuild.mp4" --ref-dir "temp/reference_cache" --cache-dir "temp/run_cache" --rebuild-step ocr -m tiny --siren-key="" --siren-voice=""
```


