# Handoff Log: repurpose-videos - 2026-06-18

**Updated**: 2026-06-18 20:15 +07:00

## Current State

`repurpose-videos` branch `v5` now includes robust, time-localized transcript-guided OCR filtering to prevent over-masking on slide decks and UI-heavy videos.

Target path:
```text
D:\eddie-projects\personal-projects\repurpose-videos
```

## Completed

- **Transcript-Guided OCR Filtering**:
  - Reordered the pipeline flow to execute Whisper transcription first (generating standard `audio.zh.srt`).
  - Modified frame OCR scan logic (`_scan_frames`) to look up active speech text in a time window $[t - 1.0\text{s}, t + 1.0\text{s}]$.
  - Developed character-level and word-level overlap check (`_check_text_overlap`) to filter out text boxes that do not match the spoken transcript.
  - Successfully prevents slide bullets, titles, watermarks, and other static/dynamic overlay UI from polluting the subtitle density profile.
- **Improved Subtitle Masking Precision**:
  - Tested on `uploads/test_video_2.mp4` containing dynamic landscape slides.
  - Subtitle zone narrowed down perfectly to `Y = 907` to `1026` (height `119px` instead of `367px`).
  - Completely resolved the issue of the blur mask climbing up and covering slide content.
- **Siren TTS Bypass**:
  - Confirmed and verified that the pipeline runs flawlessly when passing empty strings for Siren credentials (`--siren-key="" --siren-voice=""`).
- **Tests & Git**:
  - Verified 5/5 unit tests pass successfully in `tests/test_subtitle_zone.py`.
  - Pushed all working directory modifications to remote branch `v5`.

## Verification

Run unit tests:
```powershell
.venv\Scripts\python.exe tests/test_subtitle_zone.py
```
*Status*: PASS (5/5 passed)

Run local video processing command:
```powershell
.venv\Scripts\python.exe -u repurpose.py -i uploads/test_video_2.mp4 -o outputs/test_video_2_repurposed.mp4 -m tiny --siren-key="" --siren-voice=""
```
*Status*: PASS (Successfully generated repurposed video with narrow, precise blurbox at the bottom).

## Current Git State

All changes have been successfully committed and pushed to remote branch `v5`.
```text
Commit hash: 065e3ae
Message: feat(v5): implement transcript-guided OCR filtering to prevent over-masking on landscape slides
Files modified:
  - pipeline/video.py
  - repurpose.py
```

## Next Recommended Action

1. Integrate the transcript-guided OCR filtering switch/toggle or parameterize the overlap threshold in the dashboard UI if needed.
2. Verify visual results on additional sample landscape videos with different layouts.
