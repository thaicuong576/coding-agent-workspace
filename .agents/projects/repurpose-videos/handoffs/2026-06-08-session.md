# Handoff Log: repurpose-videos - 2026-06-08

**Updated**: 2026-06-08 23:05 +07:00

## Current State

`repurpose-videos` has been tidied into a deployable internal microservice layout.

Target path:

```text
D:\eddie-projects\personal-projects\repurpose-videos
```

## Completed

- Moved processor modules into `pipeline/`:
  - `pipeline/audio.py`
  - `pipeline/subtitles.py`
  - `pipeline/video.py`
- Kept root entrypoints:
  - `app.py`
  - `repurpose.py`
  - `requirements.txt`
- Added FastAPI deployment hardening:
  - `/health`
  - optional API-key auth
  - CORS allowlist
  - TrustedHost middleware
  - upload/download size limits
  - persistent job metadata
- Updated dashboard to send API keys and support protected downloads.
- Updated Dockerfile and Compose for VPS use.
- Added `docs/VPS_DEPLOY.md`.
- Updated README and project state docs.

## Verification

```powershell
$files = rg --files -g '*.py'
.venv\Scripts\python.exe -m py_compile @($files)
```

Passed.

```powershell
.venv\Scripts\python.exe repurpose.py --help
```

Passed.

FastAPI TestClient auth smoke:

```text
health 200
jobs_no_key 401
jobs_x_key 200
jobs_bearer 200
```

Docker Compose validation:

```powershell
docker compose config --quiet
```

Passed.

Live uvicorn HTTP smoke:

```text
health=200; root=200; jobs_no_key=401; jobs_with_key=200
```

## Current Git State

```text
## v2...origin/v2
 M .env.example
 M .gitignore
 M Dockerfile
 M README.md
 M app.py
 M docker-compose.yml
 M repurpose.py
 M requirements.txt
?? .dockerignore
?? docs/PROJECT_STATE.md
?? docs/VPS_DEPLOY.md
?? docs/refactoring_plan.md
?? docs/siren_talks_documentation.md
?? pipeline/
?? templates/
?? tools/
```

## Next Recommended Action

Run one full sample video through the CLI and dashboard, then stage/commit the V2 deploy-readiness changes.
