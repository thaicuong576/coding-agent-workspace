# Session Log: 2026-06-08 20:47

## Goal
Diagnose and stabilize audio-video rendering issues (silence/no sound) and test end-to-end integration via the FastAPI orchestration dashboard.

## Diagnostic Actions & Findings
1. **Audio Integrity Check**:
   - Analyzed the audio amplitudes of files generated during the pipeline run. `audio.wav`, `no_vocals.wav` (separated BGM), and `tts_voice.wav` all had significant non-zero samples and normal volume.
   - Run Whisper transcription on `video test 2_repurposed.mp4` and verified that the Vietnamese TTS speech was correctly generated and present.
2. **FFmpeg Stream Reuse Discovery**:
   - Identified a critical bug in `video_processor.py`: the TTS stream `[2:a]` was referenced twice in the filter complex (once in `sidechaincompress`, once in `amix`) without splitting it. This is undefined behavior in FFmpeg and can cause stream silent-muting or dropouts on specific platforms.
3. **Dotenv Loading**:
   - Noticed that `app.py` lacked `load_dotenv()`, which could prevent background tasks from reading Siren API keys if started directly via uvicorn.

## Fixes Implemented
1. **asplit Integration**:
   - Updated `video_processor.py` to duplicate the TTS stream using `asplit=2[tts1][tts2]` so that it can be safely consumed by both filters.
2. **Explicit Env Loading**:
   - Added `load_dotenv()` to `app.py` to guarantee credentials are loaded when the web dashboard is spun up.

## Verification
- Started the FastAPI server on port 8000.
- Sent a pipeline job with the 5s test video.
- The pipeline completed successfully in under a minute.
- Verified `outputs/d1cac264-c12_output.mp4` contains standard stereo AAC audio. Whisper successfully transcribed the voiceover track from the mixed video output.
