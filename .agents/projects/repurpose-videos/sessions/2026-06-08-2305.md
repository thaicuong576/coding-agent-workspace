# Session Log: repurpose-videos

**Date**: 2026-06-08 23:05 +07:00
**Task**: Move processors into folders and make service VPS deploy ready

## Actions

- Loaded backend and infra workspace guidance.
- Moved processor files:
  - `audio_processor.py` -> `pipeline/audio.py`
  - `subtitle_processor.py` -> `pipeline/subtitles.py`
  - `video_processor.py` -> `pipeline/video.py`
- Added `pipeline/__init__.py`.
- Updated imports in `repurpose.py` and `tools/dev/pipeline_smoke_probe.py`.
- Hardened `app.py`:
  - `/health`
  - API-key dependency
  - `X-API-Key`, Bearer token, and query-token support
  - CORS allowlist from `ALLOWED_ORIGINS`
  - TrustedHost middleware from `ALLOWED_HOSTS`
  - upload and remote download limits
  - persistent job state in `data/jobs.json`
  - configurable data/upload/output directories
  - preparation-failure cleanup for partial uploaded/downloaded files
- Updated `templates/dashboard.html` so API calls include the stored key.
- Updated Dockerfile for non-root runtime and healthcheck.
- Updated docker-compose for env-driven port and persistent volumes.
- Added `.dockerignore`.
- Added `docs/VPS_DEPLOY.md`.
- Updated README, `.env.example`, `.gitignore`, `docs/PROJECT_STATE.md`, and `docs/refactoring_plan.md`.

## Verification

```powershell
$files = rg --files -g '*.py'
.venv\Scripts\python.exe -m py_compile @($files)
```

Passed with no output.

```powershell
.venv\Scripts\python.exe repurpose.py --help
```

Passed.

```powershell
docker compose config --quiet
```

Passed with no output.

FastAPI TestClient auth smoke:

```text
health 200
jobs_no_key 401
jobs_x_key 200
jobs_bearer 200
```

Live uvicorn HTTP smoke:

```text
health=200; root=200; jobs_no_key=401; jobs_with_key=200
```

## Notes

- No full video pipeline run was performed.
- `SERVICE_API_KEY` is optional in code, but should always be set on VPS.
- `/health` is intentionally public for uptime checks.
