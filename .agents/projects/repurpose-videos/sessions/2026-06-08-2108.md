# Session Log: 2026-06-08 21:08

## Fixes This Session

### Bug: Black Shadow Remaining After Subtitle Inpainting
- **Diagnosed** by extracting raw subtitle zone frames and comparing old vs. new masks visually.
- Old mask only detected bright white text strokes (`threshold >180`). The thick black drop-shadow/outline around Chinese characters was not detected and left behind as a black smear.
- **Fix**: Dual-threshold mask in `video_processor.py`:
  - Bright mask (`>175`) for white strokes
  - Dark outline mask via Gaussian blur subtraction (`subtract(blurred, gray) > 18`)
  - Merged and dilated with `15×15` ellipse kernel (×2 iterations)
- Before: black smear clearly visible under new subtitle burn
- After: clean background repainted from surrounding environment

### Bug: FFmpeg Silent Audio / TTS Stream Reuse
- The filter complex reused `[2:a]` (TTS) twice without splitting.
- **Fix**: Added `asplit=2[tts1][tts2]` to properly duplicate TTS stream for both `sidechaincompress` and `amix`.
- Verified via Whisper transcription that TTS voiceover is audible in final output.

### Bug: Missing `load_dotenv()` in `app.py`
- Siren API key not loading when running via uvicorn.
- **Fix**: Added `load_dotenv()` at top of `app.py`.

## Verification
- Full end-to-end pipeline run completed via API (`/repurpose` POST).
- Job completed in ~52 seconds on CPU.
- Output: `outputs/d1cac264-c12_output.mp4` — confirmed clean subtitle removal + audible TTS voiceover.
