# Shared Skills

Put Eddie-specific reusable skills here.

Examples:

```text
repo-video-research/
hyperframes-preview-qa/
project-handoff/
```

Each skill should have a `SKILL.md` and any helper scripts/assets it needs.

If a runtime needs skills in a tool-specific location, mirror or link from this folder into that runtime adapter.

