# Claude Workspace Adapter

This folder is reserved for Claude Code-style workspace configuration.

Canonical source lives in:

```text
..\.agents\
```

Use `.claude/` as an adapter for Claude-specific loading conventions, not as the only source of truth.

