# Codex Boot Adapter

Welcome, Codex. This repository is Eddie's Coding Agent Workspace (home base).

Before performing any tasks or edits:
1. Read the universal boot protocol and guidelines in the root [AGENTS.md](file:///d:/eddie-agents/coding-agent-workspace/AGENTS.md).
2. Read the active project status in [.agents/state.json](file:///d:/eddie-agents/coding-agent-workspace/.agents/state.json).
3. Do not edit product code inside this workspace directory. Locate the active target project config in `.agents/projects/`, and shift your context to the target repository's path.
4. Apply the personas in `.agents/personas/`, rules in `.agents/rules/`, and skills in `.agents/skills/` to structure your plan, implementation, and verification.
