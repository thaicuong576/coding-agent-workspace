# Codex Plugin Adapter

Mirror or link shared plugin source from:

```text
..\.agents\plugins\
```

Codex's global plugin cache is not the canonical source. Use `.agents/plugins/` as the source of truth.

